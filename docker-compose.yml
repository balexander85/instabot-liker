version: '3'
services:
  web:
    image: instabot-liker:latest
    container_name: "instaliker"
    env_file: .env
    environment:
      - TZ="America/Chicago"
      - PYTHONUNBUFFERED=0
      - INSTAPY_WORKSPACE=/code/InstaPy
    volumes:
      - ./:/code

